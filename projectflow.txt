1. create git repo and clone it
2. create code.py and add code to it(it will have csv file in the folder "data")
3. Do a git add-commit-push before intializing dvc.
pip install dvc
4. now we do dvd intializing
5. now do mkdir s3 (create a new s3 directory)
6. now we do "dvc remote add -d myremote s3"
6. next 'dvc add data/'
   now it will ask to do : ('git rm -r --chached 'data'' and 'git commit -m "stop tacking data"')
   because initally we were tracking data/ folder from git so now we remove it for dvc to handle.
7. again we do 'dvc add data/' (creates data.dvc) then 'git add .gitignore data.dvc'
8. now 'dvc commit ' and then 'dvc push'
9. now we make changes to mycode.py to append a new row in data chekc changes via 'dvc status'
10. again 'dc commit' and then 'dvc push'
11. Then git 'add-commit-push (we're saving V2 of out data at this point)'
12. Check dvc/git status, everything should be upto date.
13. Now repeate step 10-12 for v3 of data.